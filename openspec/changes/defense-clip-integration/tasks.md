## 1. Script — Setup and Types

- [x] 1.1 Create `scripts/fetch_defense_clips.py` with file header, imports (`requests`, `pandas`, `json`, `pathlib`, `time`), and `OUT_PATH` pointing to `apps/web/src/data/defense-clips.json`
- [x] 1.2 Define `SEASONS = ["2024", "2023"]` and shared `HEADERS` dict (User-Agent, Accept, Referer)
- [x] 1.3 Define the 104 scenarios list: 48 infield GBs (6 positions × 8 base states), 24 outfield singles (3 positions × 8 base states), 32 doubles (4 directions × 8 base states); use `{base}:{play_type}` key format
- [x] 1.4 Mark outfield and double scenario types so the scorer knows which heuristic to apply

## 2. Script — Savant Fetch

- [x] 2.1 Implement `fetch_savant_csv(event_type, season)` — split into 2-month date chunks, retry up to 3 times per chunk, sleep 1.5s between chunks, return combined DataFrame
- [x] 2.2 Implement `filter_runners(df, base_code)` — filter on `on_1b`, `on_2b`, `on_3b` nullable columns per the 3-digit base code
- [x] 2.3 Phase 1 in `main()`: collect unique (event_type, season) pairs from all scenarios, fetch and cache each broad CSV

## 3. Script — Scoring

- [x] 3.1 Implement `score_play(row, is_outfield)` — shared: 0 outs → +3, 2024 season → +1; infield branch: low xBA (< 0.15) → +2, flat launch angle (-10° to 10°) → +1; outfield/double branch: high xBA (> 0.50) → +2
- [x] 3.2 For infield scenarios, apply `bb_type=ground_ball` post-fetch filter; for outfield and double scenarios, skip bb_type filtering

## 4. Script — Play ID Resolution

- [x] 4.1 Port `get_play_id(game_pk, at_bat_number, session)` from the prototype — call `statsapi.mlb.com/api/v1/game/{game_pk}/playByPlay`, cache per game_pk, sleep 0.1s between uncached game lookups
- [x] 4.2 Implement `video_url(play_id)` returning the full `sporty-videos` URL string

## 5. Script — Main Loop and Output

- [x] 5.1 Phase 2 in `main()`: for each scenario, merge cached DataFrames, apply post-fetch filters (location, bb_type where applicable, runners), score, take top 10, resolve play IDs
- [x] 5.2 Collect up to 3 candidates with valid play IDs per scenario; build clip dict with `play_id`, `video_url`, `game_date`, `matchup`
- [x] 5.3 Write output JSON: only include scenarios with at least 1 clip; key format `{base}:{play_type}`; pretty-print with `indent=2`
- [x] 5.4 Print summary: total scenarios, how many have ≥1 clip, how many have 3 clips

## 6. Static Data File

- [x] 6.1 Create `apps/web/src/data/` directory if it doesn't exist
- [x] 6.2 Add a placeholder `defense-clips.json` (empty object `{}`) so the TypeScript import resolves before the real fetch is run; add a comment in the file header explaining it's generated by `scripts/fetch_defense_clips.py`

## 7. Label Rename

- [x] 7.1 In `apps/web/src/lib/defense-situations.ts`, update `PLAY_TYPE_OPTIONS` for the three outfield entries: group `"Outfield Ground Ball"` → `"Outfield Single"`, labels `"Left field"` / `"Center field"` / `"Right field"` → `"Single to LF"` / `"Single to CF"` / `"Single to RF"`; leave `value` keys unchanged

## 8. UI — Clip Links and Statcast Search

- [x] 8.1 In `apps/web/src/app/defense/page.tsx`, import `defense-clips.json` with type `Record<string, Array<{ play_id: string; video_url: string; game_date: string; matchup: string }>>`
- [x] 8.2 Implement `buildSavantSearchUrl(play: PlayType, base: BaseState): string` — map play type to Savant event type and hit location params, add runner filter params; return full search URL
- [x] 8.3 Compute `const clips = defenseClips[\`${baseState}:${playType}\`] ?? []` on each render
- [x] 8.4 Render clip links row: for each clip (up to 3), render `<a href={clip.video_url} target="_blank" rel="noopener">▶ Example {n}</a>`; style consistently with the rest of the page
- [x] 8.5 Render always-present `<a href={buildSavantSearchUrl(...)} target="_blank" rel="noopener">Browse Statcast ↗</a>` in a dimmer style alongside the clip links
- [x] 8.6 Place the clip/search row in the field diagram card or directly below it — compact, does not affect diagram layout

## 9. Verification

- [x] 9.1 TypeScript type check passes (`npx tsc --noEmit`) with the imported JSON
- [x] 9.2 Run `python3 scripts/fetch_defense_clips.py` and confirm it completes without crashing; inspect output JSON for correct key format and clip structure
- [x] 9.3 Spot-check 3-5 `video_url` values — confirm they open valid Baseball Savant video pages
- [x] 9.4 Confirm the play type selector shows "Outfield Single" group with correct option labels
- [x] 9.5 Confirm clip links appear for a scenario that has clips, and Statcast search link is always visible
